name: Test Composite
description: 'How do composites work?'

inputs:
  env-name:
    required: false
    default: ''./default'

runs:
  using: "composite"
  steps:
    - name: Display Variables
      run: echo "Test Composite ENV <${{input.env-name}}>"
