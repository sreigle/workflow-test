name: Test Composite
description: "How do composites work?"

inputs:
  env-name:
    required: false
    default: "default"

runs:
  using: "composite"
  steps:
    - name: Display Variables
      shell: bash
      run: echo "Test Composite ENV"
