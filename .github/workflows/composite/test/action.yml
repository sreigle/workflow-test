name: Test Composite
description: 'How do composites work?'

inputs:
  project-url:
    required: false
    default: './default'

runs:
  using: 'composite'
  steps:

    - name: Display Variables
      run: echo "Test Composite ${{input.project-url}}"
